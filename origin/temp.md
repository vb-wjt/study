做了：
1. 删除冗余或者之前分析不佳的文档
2. 修正和补充了些问题的答案


下一步：
1. 读取 zero engine 的代码（路径：X），总结架构和功能
   1. 补充 snmp 采集信号和处理告警逻辑到文档中(注意下 snmpv3 版本的特殊处理)
   2. 补充驱动管理的功能逻辑及实现到文档中
2. 读取 mtp-core 的代码(可从别的地方获取并且复用)
3. 主要修改了原 /file/gate 下的内容，修正下当前工程中的相关文件
4. 期望对于工程中的 /tech_stack 进行整理和统一，然后放到同一个地方，不要再存在多个地方
5. refactor_pi 的内容可以先保留不处理，因为是处于进行中的研究和任务，还在变动和总结
