---
description: 回答前先输出中文「解题思路」；简单问题可跳过，复杂问题可用流程图
alwaysApply: true
---

# 解题思路透明化

收到用户问题后，**先展示分析过程，再执行工具/改代码，最后给结论**。

## 何时输出「解题思路」

| 类型 | 是否输出 | 示例 |
|------|----------|------|
| **简单** | **跳过** | 单文件位置、概念解释、一行命令、是/否确认 |
| **中等及以上** | **必须** | 跨文件排查、架构/业务分析、迁移方案、调试、多步实现 |

判断标准：是否需要读多个来源、做搜索/验证、或存在多种解读 → 输出思路。

## 「解题思路」结构（中文）

按实际需要选用小节，不必机械凑满：

1. **理解问题** — 字面需求、隐含目标、成功标准、不确定点
2. **从哪里入手** — 信息源及优先级（如 context-snapshot → 任务文档 → 代码搜索 → 读关键文件 → 跑命令），说明**为何先查它**
3. **分析路径** — 如何拆解（触点枚举、影响面、分层、基线选择等）
4. **如何验证** — 静态/动态/交叉对照；结论标注「已确认 / 高概率 / 需你确认」
5. **预期输出** — 结论形式、是否含改动建议或待决项

复杂问题可在本节内附 **mermaid 流程图** 或简短 ASCII 图，帮助看清步骤与分支。

## 执行与修正

- 思路基于**当前已知信息**；搜完发现假设错误时，在回复中写明「修正：…」并说明新方向。
- 思路块之后才是工具调用与最终答案；不要把长篇结论藏在思路里重复两遍。
- 业务拍板类待定项（文档写「待定」、多种方案等价）须标出，不替用户做决定。

## 示例（中等复杂度）

```markdown
## 解题思路

**理解**：查 IE 4440 端口在安装器中的配置来源（非运行时谁监听）。

**入手**：context-snapshot 已知触点 → `TAFCore.iap_xml` 搜 `IE_PORT` / `4440` → CustomCode 搜端口检查类。

**验证**：文档 ↔ IA 变量 ↔ properties 写入点三方对照。

**输出**：列出配置链路与删 IE 时的连带项。

---

（随后：搜索、读文件、给结论与代码引用）
```
