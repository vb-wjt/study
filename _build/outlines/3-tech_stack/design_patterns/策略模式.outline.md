<!-- auto-generated from 3-tech_stack\design_patterns\策略模式.drawio by _build/extract-drawio.ps1, do NOT edit -->

## [Page 1] Page-1

### Nodes (32)
  - ApiMdGenerator
  - metadataDefinitionContext：元数据上下文
  - apiDocTemplate：模板
  - generateJavaModelByOwner：生成 JavaModel
  - generateAllDocuments: 生成所有插件的 Md 文档
  - generateDocumentByOwner：生成 Md 文档
  - generateOpenApiDocumentByOwner：生成 OpenAPI 规范的 Json 文档
  - ......
  - 1. 类中包含生成JavaModel, Md, Json 文件的逻辑，不符合单一职责原则；当需要新增一种文档时，需要再进行修改，不符合开闭原则；
      2. 生成 Md 和 Json 文件的逻辑，构造数据的时候都依赖2个接口，需要在接口里面兼容各自的逻辑，同时改动接口之后，需要重新测试，维护成本高
  - <<Interface>>
      GeneratorStrategy
      + generator()
      + generatorByOwner(owner): Stringdefault generateByOwner(owner, template): Stringdefault saveLocalDictionart()
  - GeneratorContext
  - - generatorStrategy: GeneratorStrategy
  - + generator()+ generatorByOwner(owner): String+ generateByOwner(owner, template): String+ saveLocalDictionart()
      + convertModel(model): MetadataModel+ remark(model): MetadataModel
  - ApiMdGenerator
  - + context: MetadataDefinitionContext+ apiDocTemplate: String
  - + generateAll+ generateByOwner(owner): String
      + buildApiParams(servicesDefinition): ServiceMetadataDefinition+ buildActionParams(servicesDefinition): ServiceMetadataDefinition
      ......
  - JavaModelGenerator
  - + context: MetadataDefinitionContext+ apiDocTemplate: String
  - + generateByOwner(owner, template): String
      - buildJavaModelParams(model): MetedataModel......
  - ApiMdGenerator
  - + context: MetadataDefinitionContext+ apiDocTemplate: String
  - + generateAll+ generateByOwner(owner): String
      - buildOpenApiParams(servicesDefinition): ServiceMetadataDefinition- buildActionParams(servicesDefinition): ServiceMetadataDefinition
      - dealOpenApiParams(parms) : List<Map<String, Object>.......

### Edges (5)
  - ?  ->  ?    [1]
  - ApiMdGenerator  ->  <<Interface>>
  - JavaModelGenerator  ->  ?
  - ApiMdGenerator  ->  <<Interface>>
  - generateAllDocuments: 生成所有插件的 Md 文档  ->  ?

